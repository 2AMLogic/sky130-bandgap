# Pinned klayout-tools (`klt`) install for layout/'s DRC/LVS flow (issue #14).
#
# klt v0.1.0 on PyPI (https://pypi.org/project/klayout-tools) ships only
# the `layers`/`stats`/`cells`/`drc`/`pdk` verbs -- `extract`, `lvs`, and
# `gen` (all required by this flow) are documented but not yet released
# there. The upstream README's own "Install" section names installing
# from source as the sanctioned way to get the latest development verbs:
#
#   uv tool install git+https://github.com/2AMLogic/klayout-tools
#
# This pins an exact commit rather than floating `main`, for the same
# reproducibility discipline sim/pdk.json applies to the PDK version: a
# re-run months from now installs the identical `klt` build instead of
# whatever `main` has drifted to. Bump this pin deliberately (and re-run
# layout/bin/run-trivial-cell-flow.sh to refresh the checked-in reports)
# when picking up new `klt` capability.
#
# Bumped for issue #15 (bandgap-core floorplan): the prior pin
# (0cd630886e7b0034671d3e1e5d3131150596275f) only supports
# `gen-compose`'s `placement.strategy: "row"`, which cannot express a real
# two-dimensional floorplan (2AMLogic/klayout-tools#321). This pin adds
# `placement.strategy: "explicit"` (2AMLogic/klayout-tools#330) plus a
# `gen-compose` request-path fix (#328/#332) and byte-reproducible GDS2
# output (#325) -- all additive, no behavior change to the row strategy or
# to `gen`/`drc`/`extract`/`lvs`, confirmed by re-running
# layout/bin/run-trivial-cell-flow.sh unmodified after the bump (still
# PASS, identical verdict; see trivial-cell/reports/ for the refreshed
# record).
#
# Bumped for issue #62 (routed bandgap-core layout). The prior pin
# (67ae02ed6199c0d9c314771669fbbc925364587a) predates three capabilities this
# issue's flow depends on, all now merged upstream:
#
#   * `res_array`'s `rows` fold parameter (2AMLogic/klayout-tools#415, merged
#     via #418) -- the reason the #15 skeleton drew a reduced 16-unit R2A/R2B
#     ladder instead of the real 108. With folding, 108 units occupy ~1,231
#     um^2 instead of a ~710 um-long single row, and the real count fits the
#     area budget. This is what closes issue #62's acceptance criterion 2.
#   * the `diff_pair` nfet/pfet misclassification fix (#421, merged via #426)
#     -- before it, every `flavor: "nfet"` device was enclosed in the
#     guard-ring well tie and extracted as `pfet`. Verified effective at this
#     pin: an isolated nfet `diff_pair` now extracts `{"nfet": 8}`, and the
#     composed layout reports both flavours.
#   * the `res_array` resistor-ID marker fix (#369, merged via #382) -- a
#     drawn ladder now extracts as `res_generic_po` resistors rather than
#     plain interconnect.
#
# It also carries `klt gen-compose`'s two-pin routing, `connectivity[]` net
# labels and `pins[]` pin promotion, which is what lets the routed flow draw
# real inter-block metal and promote named top-level pins.
#
# Additive to everything the earlier flows use: re-running
# layout/bin/run-trivial-cell-flow.sh unmodified after the bump still PASSes
# with the identical verdict (see trivial-cell/reports/ for the refreshed
# record).
# Bumped again for issue #62's LVS-closure increment. The prior pin
# (680ee42edf2319aa944f66df1f6f7db3975a73c4) predates the three upstream
# fixes for the friction PR #64 filed while building the routed layout:
#
#   * 2AMLogic/klayout-tools#432 -> merged via #440. `klt gen bjt_array` now
#     draws sky130's bipolar device-recognition marker (82/44) and a well tap
#     per unit device itself, so a `bjt_array` output extracts as `pnp`
#     without help. This retires the local `klt draw` marker/tap overlay PR
#     #64 had to compose -- the overlay is deleted here, not merely disabled,
#     and the PNP count is re-measured from the generator's own geometry.
#   * 2AMLogic/klayout-tools#433 -> merged via #439. `gen-compose` no longer
#     certifies a self-net route that crosses another pad on the same block:
#     it reports the net unroutable instead of silently drawing a short. This
#     makes the "bus an array's units" failure mode *visible*, but it does
#     NOT make bussing expressible -- upstream deliberately shipped the
#     fail-visibly option only, leaving "expose a second routing metal + via
#     role" and "via-drop routing" as follow-ups (see #439's own summary).
#     `_PDK_ROLE_LAYERS["sky130"]` at this pin still has exactly one `metal`
#     role (li1 67/20) and no via role. See gen_bandgap_routed.py's
#     MET1_OVERLAY_NOTE for what this flow does about that.
#   * 2AMLogic/klayout-tools#434 -> merged via #441. `guard_ring`/`diff_pair`/
#     `bjt_array` accept `ring_gap_side`/`ring_gap_um`/`ring_gap_offset_um`,
#     cutting one routing opening through a ring band and reporting it as a
#     `GAP_<side>` port the router may cross. This fires the explicit revert
#     trigger layout/matching-plan.md Section 5a recorded: the per-matched-
#     group guard/collector rings PR #64 had to switch off are switched back
#     on here.
#
# Additive to everything the earlier flows use: re-running
# layout/bin/run-trivial-cell-flow.sh unmodified after the bump still PASSes
# with the identical four-way verdict (see trivial-cell/reports/ for the
# refreshed record).
#
# Bumped again for issue #62's post-#461 increment. The prior pin
# (6967be2d2c387b38243331ddfcc9d3495fa0bdb7) predates every upstream fix for
# the blockers the last three routed-flow records named. All five have now
# closed:
#
#   * 2AMLogic/klayout-tools#461 -> merged via #474. THE dominant blocker.
#     Every `klt gen` MOS generator used to draw the gate poly with exactly
#     the active region's extent, so the reported gate port sat on the shared
#     poly/diff boundary and no contact could be placed there legally (it
#     either straddled the diff edge or landed on poly over the channel).
#     `diff_pair`/`mos_array` now extend the poly past the diffusion into a
#     landing pad and report the gate port on the pad, so a gate is
#     contactable. This is what unblocks the six schematic nodes that
#     terminate on a gate (`VA`, `VB`, `D1`, `D2`, `GDRV`, `PN`), MOS finger
#     bussing, and therefore `devices.matched` moving off 0.
#   * 2AMLogic/klayout-tools#462 -> merged via #471. The dummy-device marker
#     suppression path was MOS-gate-only; it now covers resistors and
#     bipolars too. Measured at this pin: it changes nothing here, because
#     the path keys off `ExtractionDeck.dummy` and the sky130 curated deck
#     declares no such layer, no generator draws one, and `klt extract`
#     exposes no override. Filed as the remaining half, #491.
#   * 2AMLogic/klayout-tools#463 -> merged via #475. `gen res_array` on
#     sky130 could only draw the base `res_generic_po` flavour, while the
#     same tool's sky130 extraction deck recognises three. The schematic is
#     built on `res_high_po`; the layout draws it from this pin on, which is
#     what makes the resistor blocks a matching device class. It does NOT
#     give them a reachable bulk pad: the deck marks `res_high_po`
#     `bulk_to_substrate` and ties that terminal to its synthesized `vsubs`
#     global, which no drawn shape can join (filed as #490).
#   * 2AMLogic/klayout-tools#454 -> merged via #468. `gen-compose` now
#     exposes metal2/via1 routing roles with via-drop bussing, i.e. a second
#     routing metal above the layer every generator draws its pads on. This
#     is the capability whose absence forced this repo to hand-draw its own
#     met1 overlay (`layout/bin/met1_bus.py`, MET1_BUS_NOTE).
#   * 2AMLogic/klayout-tools#470 -> merged via #481. `klt extract` now
#     detects and reports multi-label net collisions instead of silently
#     emitting a merged `A|B` net name. The flow's own label-collision gate
#     stays in place (it is cheap and it is what caught the original), but
#     the upstream silence this issue filed against is fixed.
#
# Also carried, and relevant to this flow: #467/#472 (self-net short and
# drawn-pad-crossing detection hardening in `gen-compose`) and #477 (`klt
# lvs` degrades gracefully instead of crashing when `combine_devices()`
# raises a partial-match `RuntimeError` -- the crash this flow previously
# had to catch and fall back from itself, filed as #466).
#
# Non-regression proof for the bump, same discipline as every pin above:
# layout/bin/run-trivial-cell-flow.sh re-run unmodified still PASSes with the
# identical four-way verdict (see trivial-cell/reports/ for the refreshed
# record), and layout/bin/run-bandgap-routed-flow.sh re-run produces the
# record this increment ships. One measured difference is expected and is
# #474 itself: every MOS cell is 0.42 um taller, because the gate poly now
# extends past the diffusion into its contact landing pad (the trivial cell's
# bbox y1 goes 1.24 -> 2.08 um). Nothing else in either record moves that is
# not this increment's own doing.
#
# Bumped again for issue #62's fourteenth increment (2026-08-04). The prior
# pin (8c277eb94d6cdcb8b8b2fc02c801af3d7555e5b1) predates five upstream fixes
# that closed the friction filed while isolating AC4's remaining causes
# (issue #72's own bisect, and the last record's disclosed cause list):
#
#   * 2AMLogic/klayout-tools#490 -> merged via #495. `klt extract --deck
#     sky130` now resolves an NMOS body / `bulk_to_substrate` resistor bulk /
#     collector-less bipolar collector terminal to a real drawn `tap.drawing`
#     ring outside `nwell` when one is present and contacted, instead of
#     unconditionally synthesizing the `vsubs` global. This repo's guard-ring
#     taps on `amp_nload`/`amp_nmirr` (both wired to `VSS`) are exactly that
#     shape, so this pin removes the dependency on `hints.same_nets` for the
#     substrate correspondence where those taps are drawn -- measured below.
#   * 2AMLogic/klayout-tools#491 -> merged via #494. sky130's curated deck now
#     declares a `dummy` marker layer and `mos_array`/`res_array`/`bjt_array`
#     draw it over `dummy_cells`' own footprint, so `klt extract`'s existing
#     (since #462) dummy-device suppression can finally fire on sky130.
#   * 2AMLogic/klayout-tools#492 -> merged via #497. `mos_array`/`diff_pair`
#     gain an opt-in `gate_contact` param that draws the contact + local-metal
#     pad on the #461/#474 gate landing pad, and `gen-compose` now rejects (
#     instead of silently drawing an uncontacted stub over) a route to a bare
#     poly pin. Default is `false` (byte-identical geometry unless adopted);
#     this flow does not opt in this pass -- see the record for why.
#   * #496 -> merged via #498. `gen-compose`'s Manhattan backbone stub is now
#     widened to match a north/south port's own drawn pad width when the pad
#     is wider than `routing.width_um`, closing a same-layer-spacing DRC trap
#     next to any wide pad (e.g. this flow's `TAP_N`/`TAP_S` guard-ring taps).
#   * 2AMLogic/klayout-tools#504 -> merged via #505. `klt lvs` now reports a
#     dedicated `device.class_arity` mismatch (naming both terminal lists)
#     instead of a silent, unattributed `device.unmatched` when a layout-side
#     and reference-side device class share a name but differ in terminal
#     count -- exactly this flow's `res_high_po` (3-terminal, bulk_to_substrate)
#     vs. the reference's plain 2-terminal `R` cards. Diagnostic only, does
#     not itself make the two classes match.
#
# Non-regression proof for this bump: layout/bin/run-trivial-cell-flow.sh
# re-run unmodified still PASSes with the identical four-way verdict (see
# trivial-cell/reports/ for the refreshed record); layout/bin/run-bandgap-
# routed-flow.sh re-run produces the record this increment ships -- see that
# record and layout/matching-plan.md Section 7l for what moved and what did
# not.
#
# Bumped again for issue #62's eighteenth increment (2026-08-04). The prior
# pin (147602af31c47fb935383b1761e8ce2f21c534cf) predates
# 2AMLogic/klayout-tools#508 -> merged via #511, the friction filed by this
# issue's own seventeenth increment (layout/matching-plan.md Section 7o):
#
#   * `klayout_tools.decks.sky130.EXTRACTION_DECK` gains a third
#     connectivity level -- `metals` now `((67,20), (68,20), (69,20))`
#     (li1.drawing, met1.drawing, met2.drawing), `vias` now
#     `((67,44), (68,44))` (mcon.drawing, via.drawing -- the met1<->met2
#     via), `metal_labels` now `((67,5), (68,5), (69,5))`.
#   * `_PDK_ROLE_LAYERS["sky130"]` gains `"metal3": (69, 20)` and
#     `"via2": (68, 44)`, mirroring the existing `"metal2"`/`"via1"` pair.
#   * `gen_compose`'s router resolves `"metal3"` generically (no
#     `gen_compose.py` code change was needed upstream) but its via-drop
#     stays single-hop: a `"metal3"` backbone reaches a `"metal2"`(met1)
#     -role pin one via hop away, not a `"metal"`(li1)-role pin two hops
#     away.
#
# This is the metal-level capability Section 7o named as the only
# remaining lever for AC1's `D1`/`GDRV`/`VSS` corridor deadlock -- a
# genuinely independent second routing plane above the met1 bus this
# flow's own `met1_bus.py` already hand-routes every other net on. Picking
# it up here is deliberately scoped to the pin bump alone: extending
# `met1_bus.py`/`gen_bandgap_routed.py`'s router to actually draw on
# met2/via.drawing for the three still-unrouted hops is real new routing
# logic (via1 landing-pad geometry, met2 DRC thresholds, a second
# congestion-free candidate-path search), not a re-run, and is left for the
# next increment per this issue's own established one-lever-per-increment
# discipline (Sections 7d-7o).
#
# Non-regression proof for this bump: layout/bin/run-trivial-cell-flow.sh
# re-run unmodified still PASSes with the identical four-way verdict (see
# trivial-cell/reports/ for the refreshed record); layout/bin/run-bandgap-
# routed-flow.sh re-run reproduces the prior record's AC1/AC4 numbers
# unchanged (9/12 schematic nets, mismatch_count=32, devices.matched=6,
# nets.matched=3) -- expected, since no routing code changed, only the
# newly available metal3/via2 role is unused as of this pin.
#
# Update, nineteenth increment: the "left for the next increment" above is
# done. `met1_bus.py` gains `via1()`/`hseg2()`/`vseg2()` and
# `gen_bandgap_routed._connect_met2()` lifts a hop onto the new plane when
# no met1 form clears, which closes AC1 at 12/12 schematic inter-block nets
# (0 unrouted) and takes `mismatch_count` 32 -> 18. The pin is unchanged --
# this is layout-side routing logic on the capability this bump picked up,
# not another bump. One thing the bump did NOT bring: the curated sky130
# *DRC* deck was not extended alongside the extraction deck and carries no
# met2/via rule at all, so `layout/bin/met2_drc.py` checks the new plane
# against the installed PDK's own source rules and the flow gates on it.
# Filed upstream as 2AMLogic/klayout-tools#513. See matching-plan Section 7q.
#
# Bumped again for issue #62's twenty-second increment (2026-08-05). The
# prior pin (af5791b557fc7c669c3981335a294256ccf37e6f) predates the two
# upstream fixes that close AC4's `res_high_po` value-model cause (the last
# record's only remaining `device.property` category):
#
#   * 2AMLogic/klayout-tools#518 -> merged via #519. `ResistorDevice` gains a
#     `fixed_offset_ohm` coefficient (mirrors `CapacitorDevice.perim_cap_f_um`
#     from #512/#517): `R = L / W * sheet_rho_ohm_sq + fixed_offset_ohm`,
#     applied in `_describe_devices`. sky130's curated deck sets
#     `res_high_po`'s `sheet_rho_ohm_sq=324.827244` /
#     `fixed_offset_ohm=379.705147` (measured against the real
#     `sky130_fd_pr__res_high_po` two-term SPICE model at the `tt` corner,
#     `rel=1e-6` fit at 4 lengths plus an independent held-out cross-check --
#     see the deck's own provenance comment). This is *on by default* for
#     `res_high_po` -- no opt-in flag this repo's own code needs to pass.
#   * 2AMLogic/klayout-tools#521 -> merged via #526. #519's correction was
#     applied only inside `_describe_devices`, i.e. only to `klt extract`'s
#     JSON report -- the `kdb.Netlist` itself (what `NetlistSpiceWriter`
#     writes to the `.spice` file this flow's own `klt lvs` step reads back
#     in) kept KLayout's uncorrected single-term value. Without this second
#     fix, #519 would have been invisible to this flow's LVS comparison even
#     though the JSON report looked corrected: this flow's own
#     `bandgap_core_routed.extract.spice` is written by `klt extract` and fed
#     to `klt lvs` as two already-extracted netlists (Flow step 5/6 in every
#     prior record), not compared via `klt lvs`'s inline-extraction path --
#     but both paths share the same `kdb.Netlist`-mutation fix, so this pin
#     is required either way. Confirmed by diffing `klt extract`'s written
#     `.spice` before/after this pin: R1/R2A/R2B's `R` cards move from the
#     uncorrected body-only value to the corrected two-term value.
#
# Checked the range for anything else worth picking up in the same bump:
# `git log 2b592b5..127b52d` (klayout-tools main tip as of this writing) adds
# four more commits, none touching `decks/sky130.py`, `extract.py` or
# `lvs.py` (`git diff --stat` over that range confirms zero overlap) --
# `klt pdk macros`/`klt pdk cells` gf180mcu-model-recognition/IHP-Open-PDK
# feature work and a `.claude/commands/loom/champion-epic.md` housekeeping
# fix, unrelated to this flow's sky130 resistor extraction. Pinning at
# 39bdbc4 (immediately after #526) rather than tip, per this repo's own
# deliberate-bump discipline: it is the exact commit this increment's fix
# needs and nothing more.
#
# Non-regression proof for this bump: layout/bin/run-trivial-cell-flow.sh
# re-run unmodified still PASSes with the identical four-way verdict (see
# trivial-cell/reports/ for the refreshed record); layout/bin/run-bandgap-
# routed-flow.sh re-run produces the record this increment ships -- see that
# record for the measured effect on `mismatch_count` and the resistor
# `device.property` mismatches.
#
# Bumped again for issue #62's twenty-seventh increment (2026-08-05). The
# prior pin (39bdbc4c10f5005f0eb984a5ef89329159f9f7e2) is exactly what
# klayout-tools#559 (filed by the twenty-third increment above) predates:
#
#   * 2AMLogic/klayout-tools#559 -> merged via #583
#     (48849358a1691d97445fa5f89bd3bf727b1c90d6). `options.combine_devices`
#     folds N series-connected drawn resistor primitives into one logical
#     device by summing each primitive's R -- but #519/#526's
#     `fixed_offset_ohm` head/end-resistance correction was already baked
#     into each primitive's R at extraction time, so the fold summed it N
#     times instead of once. #583 defers the resistor correction until
#     after `combine_devices()` runs, applying it once per surviving
#     (post-fold) device object instead. This is exactly this flow's own
#     `res_r2`/`res_trim`/`res_r1` trim-tap decomposition's shape (70/70/7
#     drawn primitives per logical R2A/R2B/R1 device, RES_HEAD_RESISTANCE_NOTE)
#     -- the twenty-third increment's disclosed, measured AC4 floor.
#
#   Picking this up does NOT close AC4 either, for a different reason than
#   the twenty-third increment's: #583's deferred correction only runs on
#   `klt lvs`'s inline-extraction request shape (`layout.file`+`layout.
#   deck`); this flow's own combined-LVS step must use the pre-extracted
#   `layout.netlist` shape instead (to avoid a separate, pre-existing
#   `combine_devices()` reliability issue on this cell's bipolar array), and
#   `layout.deck` is silently ignored on that shape. Confirmed by hand that
#   the mechanism itself is correct once reachable (R2A/R2B land within
#   0.05% of the reference via the inline shape) but that switching this
#   flow to the inline shape is a net regression for an unrelated reason
#   (PNP combine breaks, `mismatch_count` 4 -> 28). Filed as friction:
#   2AMLogic/klayout-tools#585. See layout/matching-plan.md Section 7x.
#
#   [CORRECTED by the twenty-eighth increment below.] The "`layout.deck` is
#   silently ignored on that shape" claim above is FACTUALLY WRONG:
#   `layout_deck` resolves unconditionally in `run_lvs`. The real reason #583
#   alone did not reach this flow was a case-sensitivity bug (fixed by #587),
#   and -- more importantly -- reaching the once-per-device correction is not
#   even the goal (DR-003: 114,282 ohm is this layout's genuine resistance).
#   See the twenty-eighth increment block below.
#
# Checked the range for anything else worth picking up in this bump:
# `git log 39bdbc4..4884935` (klayout-tools main tip as of this writing) adds
# 17 more commits ahead of the prior pin; `git diff --stat` over that range
# shows only `#583` itself touches `extract.py`/`lvs.py` (`docs/cli/extract.md`,
# `docs/cli/lvs.md`, `src/klayout_tools/extract.py`, `src/klayout_tools/lvs.py`,
# `tests/test_lvs.py`). The other 16 commits are gf180mcu deck DRC/extract
# curation, a new `DiodeDevice` extraction family, `klt pdk macros`/`klt pdk
# corners`/`--top` CLI additions, and a `synthesize` Yosys-version guard --
# none touch `decks/sky130.py` or this flow's resistor path. Pinning at
# 4884935 (== #583's own merge commit, also klayout-tools main tip as of
# this writing) rather than an intermediate SHA, per this repo's own
# deliberate-bump discipline: it is the exact commit this increment's fix
# needs and nothing more.
#
# Non-regression proof for this bump: layout/bin/run-trivial-cell-flow.sh
# re-run unmodified still PASSes with the identical four-way verdict (see
# trivial-cell/reports/ for the refreshed record); layout/bin/run-bandgap-
# routed-flow.sh re-run produces the record this increment ships -- see that
# record for the measured effect on `mismatch_count` and the resistor
# `device.property` mismatches.
#
# Bumped again for issue #62's twenty-eighth increment (2026-08-06). The
# prior pin (48849358a1691d97445fa5f89bd3bf727b1c90d6, == #583) is bumped
# past 2AMLogic/klayout-tools#587 -> merged as acb0ae6 (also klayout-tools
# main tip as of this writing). This bump corrects the twenty-seventh
# increment's diagnosis and settles what the resistor mismatches actually
# are:
#
#   * 2AMLogic/klayout-tools#587 (closes #585/#586). #583 alone did not reach
#     this flow's pre-extracted `layout.netlist` shape -- NOT because
#     `_resolve_layout` "ignores `layout.deck`" (that claim was factually
#     wrong; `layout_deck` resolves unconditionally in `run_lvs`), but because
#     `apply_resistor_fixed_offset_corrections` keyed its device-class lookup
#     case-sensitively (`res_high_po`) while a SPICE-round-tripped netlist
#     reports the class UPPERCASED (`RES_HIGH_PO`), so the lookup silently
#     missed. #587 normalizes that lookup case-insensitively AND adds
#     `run_extract(apply_resistor_fixed_offset=False)`. With this pin, the
#     once-per-combined-device correction IS reachable on this flow's own
#     request shape (measured by hand: R2A/R2B land at 88,083 ohm).
#
#   It is deliberately NOT adopted. DR-003 (issue #98) ratified, with
#   independent real-SPICE evidence, that this layout physically pays the
#   head/end resistance once per separately-contacted instance -- R2A/R2B =
#   114,282 ohm is the genuine value, not an LVS-extraction artifact -- and
#   that the shift pushes VOUT outside the draft +/-1% window at every corner
#   checked. So reporting the single-device 88,083 would mask a real, ratified
#   design defect to make the number look better, exactly the relax-to-pass
#   CLAUDE.md refuses. The flow's LVS request shape is UNCHANGED (pre-extracted
#   `{netlist, top}`, no `deck`); it keeps reporting the physically-honest
#   114,282, and the 3 resistor `device.property` findings are a real sizing
#   defect fixed by issue #99's resize, not an LVS-accounting change. The only
#   flow diff this increment ships is this pin bump and the narrative
#   corrections (README.md, layout/README.md, matching-plan.md Section 7y,
#   gen_bandgap_routed.py) -- no generator, gate, or LVS-request logic changed.
#
# Range check: `git log 4884935..acb0ae6` adds only #587 touching
# `extract.py`/`lvs.py`; nothing else on this flow's sky130 resistor path.
#
# Non-regression proof for this bump: layout/bin/run-trivial-cell-flow.sh
# re-run unmodified still PASSes with the identical four-way verdict (see
# trivial-cell/reports/ for the refreshed record); layout/bin/run-bandgap-
# routed-flow.sh re-run reproduces `mismatch_count=4` (3 resistor
# `device.property` + 1 `device.unmatched` MCC), devices.matched=12, unchanged.
klayout-tools @ git+https://github.com/2AMLogic/klayout-tools@acb0ae6c11e667eb93ee7de272d64298517f7fab
