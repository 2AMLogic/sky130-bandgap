# `res_high_po` fixed-offset accounting variants

- Record ID: `20260806-051430-48f352b`
- Source layout record: `layout/bandgap-core/reports/20260806-051430-48f352b`
- `klt` pin: `klayout-tools @ git+https://github.com/2AMLogic/klayout-tools@acb0ae6c11e667eb93ee7de272d64298517f7fab`
- Repo state: `48f352b1bd6480968238331e378cee0b801cc12b`

Generated by `layout/bin/measure_fixed_offset_variants.py`. Reads the
source record's own drawn `.gds` and re-runs `klt lvs` under each of the
four `fixed_offset_ohm` accounting combinations. Nothing here feeds the
routed flow's gate -- this is evidence for *why* the flow keeps the
per-primitive accounting, per DR-003 (issue #98).

| variant | extraction offset | `layout.deck` | `mismatch_count` | `devices.matched` | R2A/R2B `r` (Ω) | R1 `r` (Ω) |
| --- | --- | --- | --- | --- | --- | --- |
| `primary_nodeck` | per_primitive | absent | **4** | 12 | 114,282.716170 | 14,026.889569 |
| `primary_deck` | per_primitive | `sky130` | **4** | 12 | 114,662.421317 | 14,406.594716 |
| `deferred_nodeck` | deferred | absent | **4** | 12 | 87,703.355880 | 11,368.953540 |
| `deferred_deck` | deferred | `sky130` | **4** | 12 | 88,083.061027 | 11,748.658687 |

Reference-side values, unchanged across variants: `R2A`/`R2B` = 88130.0, `R1` = 11755.0.

## What this measures

- `primary_nodeck` is the shipped flow's own request shape.
- `primary_deck` double-counts: the extractor already baked the offset
  into every primitive, and `klt lvs` adds it once more post-combine.
- `deferred_nodeck` reports body resistance only (no head/end term).
- `deferred_deck` is klayout-tools#587's intended pairing: defer at
  extraction, apply once per post-combine device.

## Conclusion recorded by this run

`mismatch_count` is **4** across the four
variants, so the deferral is not a lever on issue #62's AC4 number at all.
Adopting it would only change *which* resistance `klt lvs` prints, trading
the folded array's physically real value (DR-003, issue #98) for the
single-device one, with no gate benefit. See `layout/matching-plan.md`
Section 7y.
