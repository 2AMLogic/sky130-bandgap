# `klt erc` supply record — `20260922-235513-70ce985`

T1 item 11 (power delivery, **structural**) evidence for `bandgap-core`. Generated by `layout/bin/run-erc-supply-flow.sh`; do not hand-edit — re-run the flow to append a new record. Read [`../README.md`](../README.md) first for what this does and does not establish.

## Supply-spec runs (`erc-supply-spec.json`)

| routed GDS | GDS sha256 | report `provenance.input.content_hash` | supplies checked | `erc.unconnected_net` / `erc.supply_short` on a supply | gate |
|---|---|---|---|---|---|
| `20260811-221633-a0ee5e7` | `f2ff80dafb269c23…` | `f2ff80dafb269c23…` | 2/2 (VDD, VSS) | **0** | **PASS** |
| `20260817-020222-13476b7` | `8fa40aaae6e588e9…` | `8fa40aaae6e588e9…` | 2/2 (VDD, VSS) | **0** | **PASS** |

Newest GDS (`20260817-020222-13476b7`): `erc_status` `clean`, `erc_finding_count` 0, overall `status` `clean`, produced by `klt 0.6.0` / KLayout `0.30.12`.

**`erc.missing_tie` is NOT COMPUTED by these runs.** `erc_coverage.inapplicable` records it as `ties_disclosed_tool_limitation` — the spec declares an empty `ties[]` with a `ties_disclosure` of kind `tool_limitation`. Its zero count is an absence of evidence, not evidence of absence; the standing-in well-tie evidence is named in [`../README.md`](../README.md).

## Antenna / gate accumulation (reported, not this item's subject)

Reported here because the same run produces it. An antenna verdict is not item 11's subject and does not gate this record ([klayout-tools#1994](https://github.com/2AMLogic/klayout-tools/issues/1994)).

| gate net | `gate_area_um2` (poly ∩ diff) | `antenna_verdict` |
|---|---|---|
| `VA` | 1600.0 | `pass` |
| `VB` | 1600.0 | `pass` |
| `D2` | 1280.0 | `pass` |
| `D1` | 1280.0 | `pass` |
| `PN` | 1920.0 | `pass` |
| `GDRV` | 9664.0 | `pass` |

Device bodies carved out of the conductor roles by `--deck sky130`:

- `res_high_po` on role `poly` (`body_layer` 66/20), 587.0 µm², source `deck`

## Known-gap n-well tie reproductions (friction evidence)

Not a verdict on this layout. These two runs reproduce [klayout-tools#2339](https://github.com/2AMLogic/klayout-tools/issues/2339) against `20260817-020222-13476b7`: `ties[].well_layer` checks **every** merged shape on `nwell` 64/20 against one declared net, and this block's n-wells are not uniformly biased. Their finding sets are disjoint complements, so between them every n-well shape is shown tapped to its correct supply.

| tie spec | declared net | `erc.missing_tie` | `erc.supply_short` | findings |
|---|---|---|---|---|
| `erc-nwell-tie-spec.known-gap.vdd.json` | `VDD` | 2 | 0 | `erc.missing_tie` net=VDD layer=nwell_tie_vdd bbox (1.55, 0.82)-(12.85, 2.88) um; `erc.missing_tie` net=VDD layer=nwell_tie_vdd bbox (244.89, 0.82)-(272.51, 8.32) um |
| `erc-nwell-tie-spec.known-gap.vss.json` | `VSS` | 4 | 0 | `erc.missing_tie` net=VSS layer=nwell_tie_vss bbox (27.89, 47.6)-(36.11, 66.98) um; `erc.missing_tie` net=VSS layer=nwell_tie_vss bbox (52.11, 35.6)-(143.77, 78.98) um; `erc.missing_tie` net=VSS layer=nwell_tie_vss bbox (0, 102.83)-(171.66, 118.21) um; `erc.missing_tie` net=VSS layer=nwell_tie_vss bbox (51.2, 142.06)-(222.86, 205.44) um |

Combined `erc.missing_tie` count across both halves: **6**, which is the total n-well shape count on this layout — every well is reported by exactly one of the two halves, i.e. every well is tied, to one supply or the other. **Zero `erc.supply_short` in either half** re-confirms that [klayout-tools#2169](https://github.com/2AMLogic/klayout-tools/issues/2169)'s well-conductor collapse does not reproduce on the pinned `klt` build.

## Verdict

**PASS** — item 11's supply half.

This gate covers the supply-island half of item 11 only. The item as a whole stays **unmet** while `erc.missing_tie` is uncomputed — the machine-graded verdict of record is `signoff/signoff-report.json` (see `signoff/README.md`), which renders item 11 `supply_spec_disclosed_tool_limitation`. See [`../README.md`](../README.md) for why.

