{
  "name": "loom-workspace",
  "description": "Loom-managed workspace with AI development orchestration",
  "private": true,
  "scripts": {
    "worktree:return": "./.loom/scripts/worktree-return.sh",
    "check:ci": "npm run lint && npm run test",
    "check:all": "npm run check:ci",
    "test": "python3 -m py_compile sim/bin/corner-run.py",
    "lint": "find sim -name '*.json' -print0 | xargs -0 -n1 python3 -m json.tool > /dev/null && bash -n sim/bin/pdk-env.sh && bash -n loom.sh"
  }
}
