# Record 20260803-142259-544cc5e

- **Record ID**: 20260803-142259-544cc5e
- **Experiment**: `monte-carlo-untrimmed` — Monte Carlo mismatch sigma and yield against the untrimmed +/-1% output-accuracy window, with a PNP / resistor / amp-mirror contributor breakdown
- **Claim**: README.md 'Target specification (DRAFT)' row 'Output reference' (1.20 V +/-1% untrimmed) -- but read as a MISMATCH claim, not the corner-matrix claim issue #11 already substantiates. Process-corner sims move every device the same direction together; the untrimmed spread that actually determines whether a real part lands inside +/-1% is set by how far a real PNP, resistor, or MOS device drifts from its neighbor, i.e. LOCAL MISMATCH, which only Monte Carlo over sky130's mismatch models can quantify. Wraps issue #11's own output-voltage bench (sim/output-voltage-tc/testbench/tb_vref_tc.sch, unmodified) rather than re-deriving the v(vref) extraction. Records are PROVISIONAL against the draft spec until issue #1 ratifies it. Feeds issue #13 (does the design need a trim network, and what range) and issue #15 (layout matching priority, from the contributor breakdown).
- **Netlist provenance**: schematic (`sim/output-voltage-tc/testbench/tb_vref_tc.sch` — issue #11's testbench, wrapped unmodified; not copied or re-derived)
- **PDK**: sky130A @ open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (matches sim/pdk.json pin); models `/home/ubuntu/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
- **Tools**: ngspice-46 : Circuit level simulation program; XSCHEM V3.4.7; Linux 7.0.0-1009-aws x86_64
- **Repo state**: `544cc5e` on `feature/issue-12` (working tree dirty at run time)
- **Corner matrix run**:
  - Process: `tt_mm` only — the nominal process point with the PDK's local-mismatch switch on. In `libs.tech/combined/sky130.lib.spice` every plain corner section sets `.param MC_MM_SWITCH=0` and only the `*_mm` sections set it to 1, so **the section name is the mismatch switch**; `MC_PR_SWITCH` (global process Monte Carlo) stays 0. Local mismatch is intra-die variation, independent of (and not double-counting) the global process shift `sim/output-voltage-tc`'s 15-point corner matrix already records for this same core.
  - Temperature: -40 °C, 27 °C, 125 °C (full CLAUDE.md temperature axis, per the issue's explicit scope: nominal + both extremes, not a full corner-matrix MC run)
  - Supply: **3.30 V only**. This deck has a supply rail, so the subset needs a reason: supply is a *deterministic* axis for this circuit, already swept by `sim/output-voltage-tc` (±10 % at every process/temperature point), while every mismatch coefficient this record samples (`sw_mm_*`) is a device parameter with no supply dependence. This runner does not go through `sim/bin/corner-run.py`, so that justification is stated here rather than via `--subset-reason` (see `sim/README.md`).
  - Contributor isolation: 4 configs per temperature — all families (the spec claim), PNP array only (Q1 CTAT + Q2 PTAT), PTAT/output resistor network only (R1, R2A, R2B), amp/mirror MOS only (error_amp diff pair + mirrors + XMPOUT/XMPAMP) — by zeroing the PDK's own `sw_mm_*` coefficients for the two families NOT under test (see the runner script header for the exact mechanism and the coefficient names).
  - 14 ngspice points executed: 12 Monte Carlo points (N = 300 each, 3 temperatures x 4 configs), one MC-off control point, one second-seed point.
- **Statistical convention**: **N = 300** Monte Carlo samples per point, local mismatch only (section `tt_mm`: `MC_MM_SWITCH=1`, `MC_PR_SWITCH=0`). Spread is the sample standard deviation (N−1) reported as **1 σ**, with 3 σ alongside; σ's own relative standard error is ≈ 4.1 %. Reproducible: `setseed 20260803` (second-seed point uses 20260804). N matches `sim/pnp-mismatch` and `sim/error-amp-offset-mc`, so σ figures are directly comparable across all three. See 'Sample-count justification' below for the empirical convergence check.
- **Result**: FAIL — see the tables below.

## Output-voltage distribution and yield against the +/-1 % window

Window: [1.188, 1.212] V around the draft spec's 1.20 V nominal (README.md 'Target specification (DRAFT)'). **Yield** below is the fraction of MC draws that (a) converged to the core's real operating solution (excludes the issue #10 startup artifact — see 'mc_solve_yield' in the Checks section) and (b) landed inside that window.

| T (°C) | config | mean VOUT (V) | 1 σ (mV) | 3 σ (% of 1.20 V) | converged/total | yield (of converged) |
|---|---|---|---|---|---|---|
| -40 | all | 1.208424 | 4.8785 | 1.220% | 228/300 | 77.63% |
| -40 | pnp | 1.207397 | 1.1573 | 0.289% | 300/300 | 100.00% |
| -40 | resistor | 1.207105 | 1.6440 | 0.411% | 300/300 | 100.00% |
| -40 | mos | 1.208267 | 4.4649 | 1.116% | 230/300 | 83.48% |
| 27 | all | 1.199852 | 4.9880 | 1.247% | 300/300 | 99.00% |
| 27 | pnp | 1.199954 | 1.4104 | 0.353% | 300/300 | 100.00% |
| 27 | resistor | 1.199632 | 2.1516 | 0.538% | 300/300 | 100.00% |
| 27 | mos | 1.199700 | 4.4337 | 1.108% | 300/300 | 99.33% |
| 125 | all | 1.174690 | 5.2051 | 1.301% | 300/300 | 0.67% |
| 125 | pnp | 1.174844 | 1.9580 | 0.489% | 300/300 | 0.00% |
| 125 | resistor | 1.174491 | 2.7679 | 0.692% | 300/300 | 0.00% |
| 125 | mos | 1.174578 | 4.1965 | 1.049% | 300/300 | 0.33% |

The `all` row at each temperature is the spec-relevant number; the `pnp` / `resistor` / `mos` rows are the isolated single-family runs used for the contributor breakdown directly below, not independent yield claims of their own.

## Contributor breakdown (PNP vs. resistor vs. amp/mirror mismatch)

| T (°C) | PNP array 1 σ (mV) | resistor network 1 σ (mV) | amp/mirror MOS 1 σ (mV) | RSS of the three (mV) | measured σ(all) (mV) | closure ratio |
|---|---|---|---|---|---|---|
| -40 | 1.1573 | 1.6440 | 4.4649 | 4.8967 | 4.8785 | 0.996 |
| 27 | 1.4104 | 2.1516 | 4.4337 | 5.1260 | 4.9880 | 0.973 |
| 125 | 1.9580 | 2.7679 | 4.1965 | 5.3949 | 5.2051 | 0.965 |

At 27 °C the dominant term is **amp/mirror MOS** (4.4337 mV of the 4.4337 mV largest contributor) — this is the term issue #15's layout matching priority and issue #13's trim-range scoping should target first.

## Sample-count justification (empirical convergence check)

Running σ(VOUT) at increasing sample counts, computed from the SAME N=300, seed=20260803 draw sequence at 27 °C / `all` config (ngspice draws sequentially per `setseed`, so the first K samples of the full run are identical to what a standalone K-sample run would produce — this is a real convergence trend, not a re-simulation at each row):

| N | running σ(VOUT) (mV) | running mean (V) |
|---|---|---|
| 5 | 4.1046 | 1.202070 |
| 10 | 4.2026 | 1.200971 |
| 15 | 3.9550 | 1.200386 |
| 20 | 4.1030 | 1.200890 |

N=15 -> N=20: σ changed by 3.7% (see the `sample_count_sufficient` check below for the pass/fail threshold). Combined with the analytic standard error of a sample std-dev estimate, ≈ 4.1 % at N=300 (1/√(2(N−1))), this is why N=300 (matching the two sibling MC experiments in this repo) is treated as sufficient for the σ and yield figures above, rather than asserted by convention alone.

## Controls (the reason this record can be believed)

- **MC-off control** (`tt_27c_3.30v_mm-off`, section `tt`, N = 30): identical deck, no coefficient overrides, `MC_MM_SWITCH=0`. σ(VOUT) = 0 V — exactly zero, so the spread above is the mismatch switch and not solver noise, `.nodeset` drift, or a re-randomised bias point.
- **Second-seed point** (`tt_mm_all_27c_3.30v_seed-b`, `setseed 20260804`, N = 300): σ(VOUT) = 5.5836 mV — see the seed-stability checks below: the individual samples change, σ does not.
- **Isolation-bounded check**: every isolated-family σ (`pnp`, `resistor`, `mos`) must not exceed σ(`all`) by more than 15 % at each temperature — catches a coefficient-override that silently failed to isolate anything (see `isolation_bounded` checks below).
- **Decomposition closure**: σ(`all`) is compared against the root-sum-square of the three isolated σ's at each temperature — the three families' mismatch draws are statistically independent (different device parameters, different `AGAUSS()` terms per instance), so the total is expected to reproduce the RSS of the parts (see `decomposition_closure` checks below).
- **Startup / solve yield**: 142 of 3930 draws across all points converged to the core's *other* stable solution instead of the operating one and were excluded from every statistic (per-point counts and excluded VOUT values are in the `mc_solve_yield` check). `bandgap_core` is bistable by construction and ships no startup circuit — that is issue #10 — so a non-zero count here is a startup datum, not a mismatch one. Not a startup-yield prediction for silicon.

## Checks

- `tt_mm_all_-40c_3.30v` (mismatch, config=`all`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 228/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 72 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon. Excluded VOUT values: 0.5625 V, 0.5667 V, 0.5777 V, 0.6514 V, 0.4591 V, 0.4936 V, 0.5085 V, 0.5177 V ...
  - PASS `sigma_nonzero[vout]` — sigma = 4.8785 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 77.6% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_pnp_-40c_3.30v` (mismatch, config=`pnp`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 1.1573 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_resistor_-40c_3.30v` (mismatch, config=`resistor`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 1.6440 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_mos_-40c_3.30v` (mismatch, config=`mos`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 230/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 70 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon. Excluded VOUT values: 0.5131 V, 0.5626 V, 0.5665 V, 0.5492 V, 0.4589 V, 0.5665 V, 0.4933 V, 0.5087 V ...
  - PASS `sigma_nonzero[vout]` — sigma = 4.4649 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 83.5% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_all_27c_3.30v` (mismatch, config=`all`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 4.9880 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 99.0% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_pnp_27c_3.30v` (mismatch, config=`pnp`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 1.4104 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_resistor_27c_3.30v` (mismatch, config=`resistor`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 2.1516 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_mos_27c_3.30v` (mismatch, config=`mos`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 4.4337 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 99.3% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_all_125c_3.30v` (mismatch, config=`all`): **FAIL**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 5.2051 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - FAIL `yield_ge_50pct` — 0.7% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_pnp_125c_3.30v` (mismatch, config=`pnp`): **FAIL**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 1.9580 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - FAIL `yield_ge_50pct` — 0.0% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_resistor_125c_3.30v` (mismatch, config=`resistor`): **FAIL**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 2.7679 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - FAIL `yield_ge_50pct` — 0.0% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_mos_125c_3.30v` (mismatch, config=`mos`): **FAIL**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 4.1965 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - FAIL `yield_ge_50pct` — 0.3% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_27c_3.30v_mm-off` (control, config=`all`): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_zero[vout]` — sigma = 0 V with MC_MM_SWITCH=0 and no coefficient overrides (must be exactly 0)
- `tt_mm_all_27c_3.30v_seed-b` (seed-check, config=`all`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 5.5836 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 97.3% of converged draws land inside the +/-1% window [1.188, 1.212] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- PASS `decomposition_closure[-40C]` — sigma(all)=4.8785 mV vs RSS(pnp=1.1573, resistor=1.6440, mos=4.4649)=4.8967 mV (ratio 0.996, band 0.75-1.35); the three families' mismatch draws are statistically independent (different device parameters, different AGAUSS() terms), so the total is expected to reproduce the root-sum-square of the isolated parts
- PASS `isolation_bounded[pnp,-40C]` — sigma(pnp)=1.1573 mV must not exceed sigma(all)=4.8785 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[resistor,-40C]` — sigma(resistor)=1.6440 mV must not exceed sigma(all)=4.8785 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[mos,-40C]` — sigma(mos)=4.4649 mV must not exceed sigma(all)=4.8785 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `decomposition_closure[27C]` — sigma(all)=4.9880 mV vs RSS(pnp=1.4104, resistor=2.1516, mos=4.4337)=5.1260 mV (ratio 0.973, band 0.75-1.35); the three families' mismatch draws are statistically independent (different device parameters, different AGAUSS() terms), so the total is expected to reproduce the root-sum-square of the isolated parts
- PASS `isolation_bounded[pnp,27C]` — sigma(pnp)=1.4104 mV must not exceed sigma(all)=4.9880 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[resistor,27C]` — sigma(resistor)=2.1516 mV must not exceed sigma(all)=4.9880 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[mos,27C]` — sigma(mos)=4.4337 mV must not exceed sigma(all)=4.9880 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `decomposition_closure[125C]` — sigma(all)=5.2051 mV vs RSS(pnp=1.9580, resistor=2.7679, mos=4.1965)=5.3949 mV (ratio 0.965, band 0.75-1.35); the three families' mismatch draws are statistically independent (different device parameters, different AGAUSS() terms), so the total is expected to reproduce the root-sum-square of the isolated parts
- PASS `isolation_bounded[pnp,125C]` — sigma(pnp)=1.9580 mV must not exceed sigma(all)=5.2051 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[resistor,125C]` — sigma(resistor)=2.7679 mV must not exceed sigma(all)=5.2051 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[mos,125C]` — sigma(mos)=4.1965 mV must not exceed sigma(all)=5.2051 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `seed_sigma_stable[vout]` — sigma(seed 20260804) / sigma(seed 20260803) = 1.119 (5.5836 mV vs 4.9880 mV; tolerance +/-25%)
- PASS `seed_sample_differs[vout]` — worst-sample VOUT differs between the two seeds (1.212923 V vs 1.215362 V), i.e. the draw really did change
- PASS `sample_count_sufficient` — running sigma(VOUT) at N=15 vs N=20 (same seed stream, the 20-sample run's first 15 draws are identical to a standalone 15-sample run): 3.9550 mV -> 4.1030 mV, relative change 3.7% (tolerance 20%). Full checkpoint series: N=5: 4.1046 mV, N=10: 4.2026 mV, N=15: 3.9550 mV, N=20: 4.1030 mV
- **Overall: FAIL**

## Scope limits, stated so this record is not over-read

1. **Mismatch only, one process point.** σ(VOUT) here is the *intra-die* spread at the nominal process corner. The untrimmed ±1 % spec line also carries global process shift and temperature curvature, both already substantiated by `sim/output-voltage-tc` (issue #11). A part that passes this record's yield figure can still miss ±1 % from global corner shift alone, and vice versa.
2. **Contributor breakdown is by isolated re-run, not simultaneous internal probing.** Each family's σ comes from a separate MC run with the other two families' PDK mismatch coefficients set to zero, not from decomposing a single run's internal nodes (that approach is `sim/error-amp-offset-mc`'s, for issue #9's amplifier-offset budget specifically). The two approaches are cross-checked by the decomposition-closure band, not required to match exactly.
3. **No supply sweep.** Justified above as a deterministic axis already covered by issue #11; if a future revision finds supply-dependent mismatch coupling (e.g. through a supply-dependent bias current), that would invalidate this simplification and require a new record.

- **Links**:
  - testbench: `sim/output-voltage-tc/testbench/tb_vref_tc.sch`
  - runner: `sim/monte-carlo-untrimmed/run_mc_untrimmed.py`
  - netlist_snapshot: `sim/monte-carlo-untrimmed/netlist-snapshots/20260803-142259-544cc5e.spice`
  - logs: `sim/monte-carlo-untrimmed/corners/20260803-142259-544cc5e`
  - record_json: `sim/monte-carlo-untrimmed/records/20260803-142259-544cc5e.json`
  - device_characterization: `design/device-characterization-summary.md`
- **Timestamp / author**: 2026-08-03T14:22:59Z, ci@loom.test
- **Supersedes**: (none — first record for this claim)

Written by `sim/monte-carlo-untrimmed/run_mc_untrimmed.py`. Append-only: never edit this file — a correction is a new record with a `Supersedes` field (see `sim/README.md`).
