# Record 20260817-121131-d7d85b6

- **Record ID**: 20260817-121131-d7d85b6
- **Experiment**: `monte-carlo-untrimmed` — Monte Carlo mismatch sigma and yield against the untrimmed +/-2% output-accuracy window, with a PNP / resistor / amp-mirror contributor breakdown
- **Claim**: README.md 'Target specification (ratified -- see DR-005, issue #1)' row 'Output reference' (1.20 V +/-2% untrimmed, 3sigma mismatch MC N>=300 + process corners, -40...125 degC) -- but read as a MISMATCH claim, not the corner-matrix claim issue #11 already substantiates. Process-corner sims move every device the same direction together; the untrimmed spread that actually determines whether a real part lands inside +/-2% is set by how far a real PNP, resistor, or MOS device drifts from its neighbor, i.e. LOCAL MISMATCH, which only Monte Carlo over sky130's mismatch models can quantify. Wraps issue #11's own output-voltage bench (sim/output-voltage-tc/testbench/tb_vref_tc.sch, unmodified) rather than re-deriving the v(vref) extraction. Feeds issue #13 (does the design need a trim network, and what range) and issue #15 (layout matching priority, from the contributor breakdown).
- **Netlist provenance**: schematic (`sim/output-voltage-tc/testbench/tb_vref_tc.sch` — issue #11's testbench, wrapped unmodified; not copied or re-derived)
- **PDK**: sky130A @ open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (matches sim/pdk.json pin); models `/Users/rwalters/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
- **Tools**: ngspice-47 : Circuit level simulation program; XSCHEM V3.4.7; Darwin 25.6.0 arm64
- **Repo state**: `d7d85b6` on `feature/issue-180` (clean working tree)
- **Corner matrix run**:
  - Process: `tt_mm` only — the nominal process point with the PDK's local-mismatch switch on. In `libs.tech/combined/sky130.lib.spice` every plain corner section sets `.param MC_MM_SWITCH=0` and only the `*_mm` sections set it to 1, so **the section name is the mismatch switch**; `MC_PR_SWITCH` (global process Monte Carlo) stays 0. Local mismatch is intra-die variation, independent of (and not double-counting) the global process shift `sim/output-voltage-tc`'s 15-point corner matrix already records for this same core.
  - Temperature: -40 °C, 27 °C, 125 °C (full CLAUDE.md temperature axis, per the issue's explicit scope: nominal + both extremes, not a full corner-matrix MC run)
  - Supply: **3.30 V only**. This deck has a supply rail, so the subset needs a reason: supply is a *deterministic* axis for this circuit, already swept by `sim/output-voltage-tc` (±10 % at every process/temperature point), while every mismatch coefficient this record samples (`sw_mm_*`) is a device parameter with no supply dependence. This runner does not go through `sim/bin/corner-run.py`, so that justification is stated here rather than via `--subset-reason` (see `sim/README.md`).
  - Contributor isolation: 4 configs per temperature — all families (the spec claim), PNP array only (Q1 CTAT + Q2 PTAT), PTAT/output resistor network only (R1, R2A, R2B), amp/mirror MOS only (error_amp diff pair + mirrors + XMPOUT/XMPAMP) — by zeroing the PDK's own `sw_mm_*` coefficients for the two families NOT under test (see the runner script header for the exact mechanism and the coefficient names).
  - 14 ngspice points executed: 12 Monte Carlo points (N = 300 each, 3 temperatures x 4 configs), one MC-off control point, one second-seed point.
- **Statistical convention**: **N = 300** Monte Carlo samples per point, local mismatch only (section `tt_mm`: `MC_MM_SWITCH=1`, `MC_PR_SWITCH=0`). Spread is the sample standard deviation (N−1) reported as **1 σ**, with 3 σ alongside; σ's own relative standard error is ≈ 4.1 %. Reproducible: `setseed 20260803` (second-seed point uses 20260804). N matches `sim/pnp-mismatch` and `sim/error-amp-offset-mc`, so σ figures are directly comparable across all three. See 'Sample-count justification' below for the empirical convergence check.
- **Result**: PASS — see the tables below.

## Output-voltage distribution and yield against the +/-2 % window

Window: [1.176, 1.224] V around the ratified spec's 1.20 V nominal (README.md 'Target specification (ratified -- see DR-005, issue #1)'). **Yield** below is the fraction of MC draws that (a) converged to the core's real operating solution (excludes the issue #10 startup artifact — see 'mc_solve_yield' in the Checks section) and (b) landed inside that window.

| T (°C) | config | mean VOUT (V) | 1 σ (mV) | 3 σ (% of 1.20 V) | converged/total | yield (of converged) |
|---|---|---|---|---|---|---|
| -40 | all | 1.218840 | 6.7708 | 1.693% | 238/300 | 79.41% |
| -40 | pnp | 1.216487 | 1.2571 | 0.314% | 300/300 | 100.00% |
| -40 | resistor | 1.216731 | 2.2316 | 0.558% | 300/300 | 99.67% |
| -40 | mos | 1.218550 | 6.4030 | 1.601% | 240/300 | 84.58% |
| 27 | all | 1.211172 | 7.1449 | 1.786% | 300/300 | 95.67% |
| 27 | pnp | 1.210373 | 1.5005 | 0.375% | 300/300 | 100.00% |
| 27 | resistor | 1.210700 | 2.8329 | 0.708% | 300/300 | 100.00% |
| 27 | mos | 1.210848 | 6.5577 | 1.639% | 300/300 | 97.67% |
| 125 | all | 1.187144 | 7.3466 | 1.837% | 300/300 | 93.33% |
| 125 | pnp | 1.186323 | 1.9998 | 0.500% | 300/300 | 100.00% |
| 125 | resistor | 1.186735 | 3.6235 | 0.906% | 300/300 | 100.00% |
| 125 | mos | 1.186749 | 6.3354 | 1.584% | 300/300 | 95.33% |

The `all` row at each temperature is the spec-relevant number; the `pnp` / `resistor` / `mos` rows are the isolated single-family runs used for the contributor breakdown directly below, not independent yield claims of their own.

## Contributor breakdown (PNP vs. resistor vs. amp/mirror mismatch)

| T (°C) | PNP array 1 σ (mV) | resistor network 1 σ (mV) | amp/mirror MOS 1 σ (mV) | RSS of the three (mV) | measured σ(all) (mV) | closure ratio |
|---|---|---|---|---|---|---|
| -40 | 1.2571 | 2.2316 | 6.4030 | 6.8963 | 6.7708 | 0.982 |
| 27 | 1.5005 | 2.8329 | 6.5577 | 7.2994 | 7.1449 | 0.979 |
| 125 | 1.9998 | 3.6235 | 6.3354 | 7.5674 | 7.3466 | 0.971 |

At 27 °C the dominant term is **amp/mirror MOS** (6.5577 mV of 7.1449 mV measured σ(all)) — this is the term issue #15's layout matching priority and issue #13's trim-range scoping should target first.

## Sample-count justification (empirical convergence check)

Running σ(VOUT) at increasing sample counts, computed from the SAME N=300, seed=20260803 draw sequence at 27 °C / `all` config (ngspice draws sequentially per `setseed`, so the first K samples of the full run are identical to what a standalone K-sample run would produce — this is a real convergence trend, not a re-simulation at each row):

| N | running σ(VOUT) (mV) | running mean (V) |
|---|---|---|
| 5 | 7.8719 | 1.211084 |
| 10 | 8.7690 | 1.211969 |
| 15 | 9.2992 | 1.209441 |
| 20 | 8.1885 | 1.210139 |

N=15 -> N=20: σ changed by 11.9% (see the `sample_count_sufficient` check below for the pass/fail threshold). Combined with the analytic standard error of a sample std-dev estimate, ≈ 4.1 % at N=300 (1/√(2(N−1))), this is why N=300 (matching the two sibling MC experiments in this repo) is treated as sufficient for the σ and yield figures above, rather than asserted by convention alone.

## Controls (the reason this record can be believed)

- **MC-off control** (`tt_27c_3.30v_mm-off`, section `tt`, N = 30): identical deck, no coefficient overrides, `MC_MM_SWITCH=0`. σ(VOUT) = 0 V — exactly zero, so the spread above is the mismatch switch and not solver noise, `.nodeset` drift, or a re-randomised bias point.
- **Second-seed point** (`tt_mm_all_27c_3.30v_seed-b`, `setseed 20260804`, N = 300): σ(VOUT) = 6.7342 mV — see the seed-stability checks below: the individual samples change, σ does not.
- **Isolation-bounded check**: every isolated-family σ (`pnp`, `resistor`, `mos`) must not exceed σ(`all`) by more than 15 % at each temperature — catches a coefficient-override that silently failed to isolate anything (see `isolation_bounded` checks below).
- **Decomposition closure**: σ(`all`) is compared against the root-sum-square of the three isolated σ's at each temperature — the three families' mismatch draws are statistically independent (different device parameters, different `AGAUSS()` terms per instance), so the total is expected to reproduce the RSS of the parts (see `decomposition_closure` checks below).
- **Startup / solve yield**: 122 of 3930 draws across all points converged to the core's *other* stable solution instead of the operating one and were excluded from every statistic (per-point counts and excluded VOUT values are in the `mc_solve_yield` check). `bandgap_core` is bistable by construction and ships no startup circuit — that is issue #10 — so a non-zero count here is a startup datum, not a mismatch one. Not a startup-yield prediction for silicon.

## Checks

- `tt_mm_all_-40c_3.30v` (mismatch, config=`all`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 238/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 62 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon. Excluded VOUT values: 0.5614 V, 0.5047 V, 0.5243 V, 0.5017 V, 0.5071 V, 0.5349 V, 0.5663 V, 0.4508 V ...
  - PASS `sigma_nonzero[vout]` — sigma = 6.7708 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 79.4% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_pnp_-40c_3.30v` (mismatch, config=`pnp`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 1.2571 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_resistor_-40c_3.30v` (mismatch, config=`resistor`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 2.2316 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 99.7% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_mos_-40c_3.30v` (mismatch, config=`mos`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 240/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 60 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon. Excluded VOUT values: 0.5613 V, 0.6446 V, 0.5533 V, 0.5016 V, 0.5309 V, 0.5073 V, 0.5345 V, 0.5663 V ...
  - PASS `sigma_nonzero[vout]` — sigma = 6.4030 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 84.6% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_all_27c_3.30v` (mismatch, config=`all`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 7.1449 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 95.7% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_pnp_27c_3.30v` (mismatch, config=`pnp`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 1.5005 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_resistor_27c_3.30v` (mismatch, config=`resistor`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 2.8329 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_mos_27c_3.30v` (mismatch, config=`mos`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 6.5577 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 97.7% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_all_125c_3.30v` (mismatch, config=`all`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 7.3466 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 93.3% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_pnp_125c_3.30v` (mismatch, config=`pnp`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 1.9998 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_resistor_125c_3.30v` (mismatch, config=`resistor`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 3.6235 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 100.0% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_mm_mos_125c_3.30v` (mismatch, config=`mos`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 6.3354 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 95.3% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- `tt_27c_3.30v_mm-off` (control, config=`all`): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_zero[vout]` — sigma = 0 V with MC_MM_SWITCH=0 and no coefficient overrides (must be exactly 0)
- `tt_mm_all_27c_3.30v_seed-b` (seed-check, config=`all`): **PASS**
  - PASS `sample_count` — parsed 300 samples, expected 300
  - PASS `mc_solve_yield` — 300/300 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from sigma/yield below. This is a STARTUP observation (issue #10 -- the core has no startup circuit), not a mismatch one, and is also sensitive to the testbench `.nodeset`; not a startup-yield prediction for silicon.
  - PASS `sigma_nonzero[vout]` — sigma = 6.7342 mV (a zero here means the mismatch switch, or this config's coefficient set, never reached the models)
  - PASS `yield_ge_50pct` — 97.7% of converged draws land inside the +/-2% window [1.176, 1.224] V (sanity floor only -- the yield number itself is the deliverable, not a pass/fail spec claim; see the Result section)
- PASS `decomposition_closure[-40C]` — sigma(all)=6.7708 mV vs RSS(pnp=1.2571, resistor=2.2316, mos=6.4030)=6.8963 mV (ratio 0.982, band 0.75-1.35); the three families' mismatch draws are statistically independent (different device parameters, different AGAUSS() terms), so the total is expected to reproduce the root-sum-square of the isolated parts
- PASS `isolation_bounded[pnp,-40C]` — sigma(pnp)=1.2571 mV must not exceed sigma(all)=6.7708 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[resistor,-40C]` — sigma(resistor)=2.2316 mV must not exceed sigma(all)=6.7708 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[mos,-40C]` — sigma(mos)=6.4030 mV must not exceed sigma(all)=6.7708 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `decomposition_closure[27C]` — sigma(all)=7.1449 mV vs RSS(pnp=1.5005, resistor=2.8329, mos=6.5577)=7.2994 mV (ratio 0.979, band 0.75-1.35); the three families' mismatch draws are statistically independent (different device parameters, different AGAUSS() terms), so the total is expected to reproduce the root-sum-square of the isolated parts
- PASS `isolation_bounded[pnp,27C]` — sigma(pnp)=1.5005 mV must not exceed sigma(all)=7.1449 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[resistor,27C]` — sigma(resistor)=2.8329 mV must not exceed sigma(all)=7.1449 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[mos,27C]` — sigma(mos)=6.5577 mV must not exceed sigma(all)=7.1449 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `decomposition_closure[125C]` — sigma(all)=7.3466 mV vs RSS(pnp=1.9998, resistor=3.6235, mos=6.3354)=7.5674 mV (ratio 0.971, band 0.75-1.35); the three families' mismatch draws are statistically independent (different device parameters, different AGAUSS() terms), so the total is expected to reproduce the root-sum-square of the isolated parts
- PASS `isolation_bounded[pnp,125C]` — sigma(pnp)=1.9998 mV must not exceed sigma(all)=7.3466 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[resistor,125C]` — sigma(resistor)=3.6235 mV must not exceed sigma(all)=7.3466 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `isolation_bounded[mos,125C]` — sigma(mos)=6.3354 mV must not exceed sigma(all)=7.3466 mV by more than 115% -- catches a coefficient-override that silently failed to isolate anything (i.e. still reads the full combined spread)
- PASS `seed_sigma_stable[vout]` — sigma(seed 20260804) / sigma(seed 20260803) = 0.943 (6.7342 mV vs 7.1449 mV; tolerance +/-25%)
- PASS `seed_sample_differs[vout]` — worst-sample VOUT differs between the two seeds (1.228211 V vs 1.232991 V), i.e. the draw really did change
- PASS `sample_count_sufficient` — running sigma(VOUT) at N=15 vs N=20 (same seed stream, the 20-sample run's first 15 draws are identical to a standalone 15-sample run): 9.2992 mV -> 8.1885 mV, relative change 11.9% (tolerance 20%). Full checkpoint series: N=5: 7.8719 mV, N=10: 8.7690 mV, N=15: 9.2992 mV, N=20: 8.1885 mV
- **Overall: PASS**

## Scope limits, stated so this record is not over-read

1. **Mismatch only, one process point.** σ(VOUT) here is the *intra-die* spread at the nominal process corner. The untrimmed ±2 % spec line (DR-005's ratified accuracy row) also carries global process shift and temperature curvature, both already substantiated by `sim/output-voltage-tc` (issue #11). A part that passes this record's yield figure can still miss ±2 % from global corner shift alone, and vice versa.
2. **Contributor breakdown is by isolated re-run, not simultaneous internal probing.** Each family's σ comes from a separate MC run with the other two families' PDK mismatch coefficients set to zero, not from decomposing a single run's internal nodes (that approach is `sim/error-amp-offset-mc`'s, for issue #9's amplifier-offset budget specifically). The two approaches are cross-checked by the decomposition-closure band, not required to match exactly.
3. **No supply sweep.** Justified above as a deterministic axis already covered by issue #11; if a future revision finds supply-dependent mismatch coupling (e.g. through a supply-dependent bias current), that would invalidate this simplification and require a new record.
4. **Trimmed ±0.5 % row not evidenced.** This record grades only the *untrimmed* ±2 % window (DR-005's first accuracy row). DR-005's second, *trimmed* ±0.5 % row has no Monte Carlo evidence on file anywhere in this repo: `tb_vref_tc.sch` (and therefore this bench, which wraps it unmodified) does not model issue #13's trim network at all, so there is no trimmed operating point to sample. This is a scope gap, stated rather than silently left implicit — closing it is issue #13's job (does the design need a trim network, and what range) once that network exists to sample.

## klt yield envelope (klayout-tools, issue #180)

Machine-checkable envelope emitted by `sim/monte-carlo-untrimmed/emit_klt_yield.py` from this record's own converged `all`-config `vout` samples (the spec-relevant row; see the yield table above) -- combined with, not instead of, the process-corner evidence (`sim/output-voltage-tc`, issue #11), per klayout-tools#344. `klt yield` reports its own confidence interval, Cp/Cpk, sigma-to-spec, and a required-sample-size verdict alongside the point estimate.

| measurement | n | klt empirical yield | 95% CI | Cpk | sample-size verdict |
|---|---|---|---|---|---|
| `vout_all_m40c` | 238 | 0.7941 | [0.7371, 0.8436] | 0.2540 | insufficient |
| `vout_all_27c` | 300 | 0.9567 | [0.9270, 0.9767] | 0.5985 | insufficient |
| `vout_all_125c` | 300 | 0.9333 | [0.8989, 0.9588] | 0.5056 | insufficient |

`klt yield` warnings (carried through verbatim, not summarized away): measurement 'vout_all_m40c': the sample set is not consistent with a normal distribution (Anderson-Darling A2* = 1.3451 > 0.787); treat the parametric yield estimate and Cpk as indicative only and prefer the empirical estimate measurement 'vout_all_m40c': N = 238 resolves the yield only to +/-0.0533 at 95% confidence; 6281 samples are needed for the requested +/-0.01 measurement 'vout_all_27c': N = 300 resolves the yield only to +/-0.0248 at 95% confidence; 1593 samples are needed for the requested +/-0.01 measurement 'vout_all_125c': N = 300 resolves the yield only to +/-0.0299 at 95% confidence; 2391 samples are needed for the requested +/-0.01

- **Links**:
  - testbench: `sim/output-voltage-tc/testbench/tb_vref_tc.sch`
  - runner: `sim/monte-carlo-untrimmed/run_mc_untrimmed.py`
  - netlist_snapshot: `sim/monte-carlo-untrimmed/netlist-snapshots/20260817-121131-d7d85b6.spice`
  - logs: `sim/monte-carlo-untrimmed/corners/20260817-121131-d7d85b6`
  - record_json: `sim/monte-carlo-untrimmed/records/20260817-121131-d7d85b6.json`
  - device_characterization: `design/device-characterization-summary.md`
  - klt_yield_input: `sim/monte-carlo-untrimmed/records/20260817-121131-d7d85b6-klt-yield-input.json`
  - klt_yield_envelope: `sim/monte-carlo-untrimmed/records/20260817-121131-d7d85b6-klt-yield.json`
- **Timestamp / author**: 2026-08-17T12:11:31Z, ci@loom.test
- **Supersedes**: 20260816-091855-69a8867

Written by `sim/monte-carlo-untrimmed/run_mc_untrimmed.py`. Append-only: never edit this file — a correction is a new record with a `Supersedes` field (see `sim/README.md`).
