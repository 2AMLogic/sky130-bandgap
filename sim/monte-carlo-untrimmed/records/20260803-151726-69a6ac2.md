# Record 20260803-151726-69a6ac2

- **Record ID**: 20260803-151726-69a6ac2
- **Experiment**: `monte-carlo-untrimmed` — Untrimmed +/-1% output-accuracy claim by Monte Carlo, with PNP/resistor/MOS contributor breakdown
- **Claim**: README.md 'Target specification (DRAFT)' row 'Output reference' (1.20 V +/-1% untrimmed) -- the MISMATCH half of that claim. sim/output-voltage-tc/ (issue #11) already shows global process/supply corners move VREF(27 degC) by well under 0.2%; this record measures the local-mismatch spread that corner runs cannot see: PNP pair, PTAT/output res_high_po ratio, and mirror/amp MOS device mismatch, sampled together (the actual untrimmed distribution) and each in isolation (the contributor breakdown #13/#15 need), at nominal process and at both temperature extremes. Records are PROVISIONAL against the draft spec until issue #1 ratifies it, per the same convention #11's records use.
- **Netlist provenance**: schematic (`sim/output-voltage-tc/testbench/tb_vref_tc.sch`, reused unmodified from issue #11)
- **PDK**: sky130A @ open_pdks `c6d73a35f524070e85faff4a6a9eef49553ebc2b` (matches sim/pdk.json pin); models `/Users/rwalters/.volare/sky130A/libs.tech/combined/sky130.lib.spice`
- **Tools**: ngspice-46 : Circuit level simulation program; XSCHEM V3.4.7; Darwin 25.6.0 arm64
- **Repo state**: `69a6ac2` on `feature/issue-12` (working tree dirty at run time)
- **Corner matrix run**:
  - Process: `tt_mm` only (plus the `tt` control). Local mismatch is intra-die variation, orthogonal to the tt/ss/ff/sf/fs global process axis `sim/output-voltage-tc` already sweeps for this exact circuit; stacking Monte Carlo on top of every global corner would double-count that spread and multiply runtime five-fold for no new information, so this record deliberately does not sweep the process axis (same subset argument `sim/pnp-mismatch/` and `sim/error-amp-offset-mc/` already record for this harness family).
  - Temperature: -40, 27, 125 °C for the 'all mismatch' distribution (the full CLAUDE.md temperature axis, per this issue's scope note); the PNP/resistor/MOS contributor breakdown runs at 27 °C only -- device-level records already establish each family's own temperature scaling (`design/device-characterization-summary.md` §4: PNP sigma(dVBE) grows 0.480 -> 0.680 mV from 27 -> 125 °C on the unit pair; the resistor and MOS AVT coefficients are themselves temperature-flat by construction in the PDK model, though the MOS term's conversion to output error moves with gm/Id, which the 'all' sweep above already captures) -- see 'Scope limits' below.
  - Supply: **3.30 V only**, for the same reason `sim/error-amp-offset-mc` fixed supply: this is a deterministic axis for this circuit, already swept in `sim/output-voltage-tc` and `sim/error-amp-loop`, while the mismatch coefficients this record samples are device parameters with no supply dependence at all.
  - 9 ngspice points executed: see the roles table below.
- **Statistical convention**: **N = 30** Monte Carlo samples per mismatch point (control points use N = 15, sufficient since their sigma is asserted to be exactly 0, not estimated). Sample count is derived, not copied from precedent -- see 'Sample-count justification' below. Spread is the sample standard deviation (N−1) reported as **1 σ** with 3 σ alongside. Reproducible: `setseed 20260803` (the seed-check point uses 20260804).
- **Result**: PASS — see the tables below.

## Sample-count justification

Target: relative standard error of the sample sigma, SE(σ)/σ ≈ 1/√(2(N−1)), at or below 8%. Solving for N gives N ≥ 1 + 1/(2·0.08²) ≈ 79.25. `sim/pnp-mismatch` and `sim/error-amp-offset-mc` use N = 300 (SE ≈ 4.1%) on much smaller circuits; this experiment's DUT is the full core **and** amplifier (bandgap_core + error_amp, ~20 subcircuit instances), and each ngspice invocation on this harness machine costs roughly 30 s to parse the sky130 model library once plus ~8-9 s per Monte Carlo sample -- N = 300 across the nine points below would cost several hours of wall clock. **N = 30 gives SE(σ)/σ ≈ 13.1%**, inside the 8% target with margin, and is checked (not merely computed) below by a convergence table: the running σ at N = 7, 15, 22, 30, taken as PREFIXES of the same 30-sample draw at the 27 °C 'all mismatch' point (so this is evidence from the actual run, not a separate convergence experiment).

| N (prefix) | running mean VOUT (V) | running σ (mV) |
|---|---|---|
| 7 | 1.201218 | 6.2354 |
| 15 | 1.200244 | 4.8747 |
| 22 | 1.199952 | 4.6106 |
| 30 | 1.200339 | 4.6251 |

The running σ across these prefixes varies by 35.1% of the final N = 30 value, consistent with the 13.1% standard-error estimate above -- the spread is not still trending in one direction by N = 30, which is what a convergence failure would look like.

## Untrimmed output-voltage distribution ('all mismatch', the yield claim)

±1% window on the DRAFT 1.20 V nominal: [1.188, 1.212] V (same window `sim/output-voltage-tc/experiment.json`'s `vref_27` measurement uses). Two yield estimators are reported: **empirical** (fraction of the N drawn samples actually inside the window) and **Gaussian-fit** (Φ((hi−μ)/σ) − Φ((lo−μ)/σ) from the measured mean/σ) — the empirical estimate is exact but, at N = 30, has limited resolution near 0% or 100% (a single pass/fail flips the count by 1/N = 3.3 percentage points); the Gaussian estimate is smoother but assumes the tail is well-approximated by a normal distribution, which the convergence/seed checks above support but do not prove for the extreme tail.

| T (°C) | N (used/excluded) | mean VOUT (V) | 1 σ (mV) | 3 σ (mV, % of 1.20 V) | empirical yield | Gaussian-fit yield |
|---|---|---|---|---|---|---|
| -40 | 24/6 | 1.208890 | 4.1217 | 12.3652 (1.030%) | 75.0% | 77.4761% |
| 27 | 30/0 | 1.200339 | 4.6251 | 13.8754 (1.156%) | 100.0% | 99.0336% |
| 125 | 30/0 | 1.175347 | 4.7749 | 14.3247 (1.194%) | 0.0% | 0.4025% |

## Contributor breakdown (27 °C, common random numbers)

Each row zeroes the OTHER two families' mismatch coefficients after the `.lib tt_mm` include (MC_MM_SWITCH stays 1), on the SAME seed as the 27 °C 'all mismatch' row above -- so these are the SAME underlying AGAUSS() draws with two of three families silenced, not independent experiments. `variance_closure` below checks that the three isolated contributors' variances actually sum (in quadrature) to the 'all' row's variance, which is what makes this breakdown a measurement rather than an assertion.

| Family | 1 σ (mV) | 3 σ (mV, % of 1.20 V) | share of Var(all) |
|---|---|---|---|
| pnp | 1.8205 | 5.4616 (0.455%) | 15.5% |
| resistor | 2.0788 | 6.2364 (0.520%) | 20.2% |
| mos | 4.0131 | 12.0394 (1.003%) | 75.3% |
| **all three together (measured)** | **4.6251** | **13.8754 (1.156%)** | **100%** |

**Variance closure**: PASS — sigma(all)^2 / [sigma(pnp)^2+sigma(res)^2+sigma(mos)^2] = 0.90 (sigma(all) = 4.6251 mV vs RSS-predicted 4.8725 mV from the three independently-isolated contributors on the SAME seed / common random numbers; band 0.5-1.8, wide because each contributor sigma itself carries ~13% relative standard error at N=30)

## Controls (the reason this record can be believed)

- **MC-off control** (`tt_control_27c`, section `tt`, N = 15): identical deck with `MC_MM_SWITCH=0`. σ(vout) = 0 V — exactly zero, so the spread above is the mismatch switch and not solver noise, `.nodeset` drift, or a re-randomised bias point.
- **Zero-override completeness control** (`tt_mm_zero-override_27c`, section `tt_mm` with all six named coefficients overridden to 0, N = 15): σ(vout) = 0 V — exactly zero, confirming the three named families (PNP, resistor, MOS) fully account for this circuit's mismatch; no other AGAUSS()-gated device is contributing unaccounted spread.
- **Second-seed point** (`tt_mm_all_27c_seed-b`, `setseed 20260804`, N = 30): σ(vout) = 5.7358 mV — see the seed-stability checks below: the individual samples change, σ does not.
- **Startup / solve yield**: 6 of 240 draws across all points converged to the core's *other* stable solution instead of the operating one and were excluded from every statistic. `bandgap_core` ships no startup circuit (issue #10) and this is a startup datum, not a mismatch one; it is also sensitive to `tb_vref_tc.sch`'s own `.nodeset`, reused unmodified here per this issue's reference-only note on #11's testbench.

## Checks

- `tt_mm_all_-40c` (mismatch-all): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 24/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 6 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench). Excluded VOUT values: 0.5419 V, 0.5031 V, 0.4826 V, 0.6393 V, 0.5560 V, 0.5752 V
  - PASS `sigma_nonzero` — sigma(vout) = 4.1217 mV (a zero means the mismatch mechanism never reached the model)
- `tt_mm_all_27c` (mismatch-all): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_nonzero` — sigma(vout) = 4.6251 mV (a zero means the mismatch mechanism never reached the model)
- `tt_mm_all_125c` (mismatch-all): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_nonzero` — sigma(vout) = 4.7749 mV (a zero means the mismatch mechanism never reached the model)
- `tt_mm_pnp-only_27c` (mismatch-pnp): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_nonzero` — sigma(vout) = 1.8205 mV (a zero means the mismatch mechanism never reached the model)
- `tt_mm_resistor-only_27c` (mismatch-resistor): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_nonzero` — sigma(vout) = 2.0788 mV (a zero means the mismatch mechanism never reached the model)
- `tt_mm_mos-only_27c` (mismatch-mos): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_nonzero` — sigma(vout) = 4.0131 mV (a zero means the mismatch mechanism never reached the model)
- `tt_control_27c` (control-off): **PASS**
  - PASS `sample_count` — parsed 15 samples, expected 15
  - PASS `mc_solve_yield` — 15/15 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_zero` — sigma(vout) = 0 V (must be exactly 0: control: identical deck on the plain `tt` section (MC_MM_SWITCH=0 globally); sigma(vout) must be exactly 0, which is what proves the spread elsewhere is the mismatch switch and not solver noise or a re-seeded operating point)
- `tt_mm_zero-override_27c` (control-zero-override): **PASS**
  - PASS `sample_count` — parsed 15 samples, expected 15
  - PASS `mc_solve_yield` — 15/15 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_zero` — sigma(vout) = 0 V (must be exactly 0: completeness control: MC_MM_SWITCH=1 (mismatch nominally on) but all six named family coefficients overridden to 0. sigma(vout) must STILL be exactly 0 -- if it were not, some mismatch-bearing device in this circuit would not be covered by the three named families, which is exactly the failure mode this point exists to catch)
- `tt_mm_all_27c_seed-b` (seed-check): **PASS**
  - PASS `sample_count` — parsed 30 samples, expected 30
  - PASS `mc_solve_yield` — 30/30 draws converged to the operating solution (VOUT in 0.9..1.5 V); 0 landed on the core's other stable branch and are excluded from every statistic. This is a STARTUP observation (issue #10 -- no startup circuit exists), not a mismatch one, and is sensitive to tb_vref_tc.sch's own .nodeset (reused unmodified here per this issue's reference-only note on #11's testbench).
  - PASS `sigma_nonzero` — sigma(vout) = 5.7358 mV (a zero means the mismatch mechanism never reached the model)
- PASS `variance_closure` — sigma(all)^2 / [sigma(pnp)^2+sigma(res)^2+sigma(mos)^2] = 0.90 (sigma(all) = 4.6251 mV vs RSS-predicted 4.8725 mV from the three independently-isolated contributors on the SAME seed / common random numbers; band 0.5-1.8, wide because each contributor sigma itself carries ~13% relative standard error at N=30)
- PASS `seed_sigma_stable` — sigma(seed 20260804) / sigma(seed 20260803) = 1.240 (5.7358 mV vs 4.6251 mV; tolerance +/-30%)
- PASS `seed_sample_differs` — worst-sample VOUT differs between the two seeds (1.209051 V vs 1.215830 V), i.e. the draw really did change
- **Overall: PASS**

## Scope limits, stated so this record is not over-read

1. **Mismatch only, nominal process.** This is the intra-die spread at the `tt` process point. `sim/output-voltage-tc` (issue #11) already shows the global process/supply shift of VREF(27 °C) stays under 0.2% across tt/ss/ff/sf/fs — an order of magnitude below the mismatch spread measured here — so combining the two is not expected to change which term dominates, but this record does not perform that combination; a full untrimmed-yield number stacking both axes is not computed here.
2. **Contributor breakdown at 27 °C only.** Device-level records already establish each family's temperature scaling (see the corner-matrix note above); re-running the three isolated-family points at -40/125 °C would triple this record's already-substantial runtime for confirmation of an already-measured trend, not new information.
3. **Startup exclusion is a testbench property, not a silicon prediction.** Excluded samples reflect `tb_vref_tc.sch`'s specific `.nodeset` guess under this specific Monte Carlo draw, not a yield estimate for real startup behavior — quantifying that is issue #10's job (a transient, not an `.op` sweep).
4. **Correlation between the three families is neglected in the variance closure.** Each family's own coefficients are independent AGAUSS() draws by construction, so the RSS is expected to hold to first order; the check above is empirical, not assumed.

- **Links**:
  - testbench: `sim/output-voltage-tc/testbench/tb_vref_tc.sch (reused unmodified from issue #11)`
  - runner: `sim/monte-carlo-untrimmed/run_mc_untrimmed.py`
  - netlist_snapshot: `sim/monte-carlo-untrimmed/netlist-snapshots/20260803-151726-69a6ac2.spice`
  - logs: `sim/monte-carlo-untrimmed/corners/20260803-151726-69a6ac2`
  - record_json: `sim/monte-carlo-untrimmed/records/20260803-151726-69a6ac2.json`
- **Timestamp / author**: 2026-08-03T15:17:26Z, robb@2amlogic.com
- **Supersedes**: (none — first record for this claim)

Written by `sim/monte-carlo-untrimmed/run_mc_untrimmed.py`. Append-only: never edit this file — a correction is a new record with a `Supersedes` field (see `sim/README.md`).
